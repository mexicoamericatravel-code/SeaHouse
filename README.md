# Casa Brisa del Mar

Fixed Netlify build version.

## Build
npm install
npm run build

## Netlify
Build command: npm run build
Publish directory: dist

## Fix applied
Removed the unsupported lucide-react icon import that caused the Netlify build to fail.
